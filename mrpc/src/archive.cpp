#include <archive.hpp>

namespace isu
{
	std::size_t _count_size()
	{
		return 0;
	}

	std::size_t archive_to_impl(void* buf, std::size_t buf_size)
	{
		return 0;
	}

	void rarchive_impl(const void* buf, std::size_t buf_size)
	{

	}

}